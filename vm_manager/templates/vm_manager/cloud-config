## template: jinja
#cloud-config

users:
- default
- name: {{ username }}
  passwd: {{ password }}
  lock_passwd: false
  gecos: VDI User
  groups:
  - sudo
  shell: /bin/bash
  sudo: 'ALL=(ALL) NOPASSWD:ALL'
  ssh-authorized-keys: {% verbatim %}{{ v1.public_ssh_keys }}{% endverbatim %}

phone_home:
  url: {{ phone_home_url }}
  post:
    - instance_id
    - hostname
  tries: 10
